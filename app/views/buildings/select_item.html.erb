<%= turbo_frame_tag 'building-inventory' do %>
  <%= link_to 'Cancel', game_path %>
  <ul>
    <% @current_user.inventory.items.each do |item| %>
      <li>
        <%= level_color(item.level, link_to(item.full_name, building_add_item_path(@building, item))) %>
      </li>
    <% end %>
  </ul>
<% end %>