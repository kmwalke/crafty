<%= turbo_frame_tag 'building-sales-listing' do %>
  <p>
    Really purchase <%= @listing.item.stack_amount %>x <%= @listing.item.full_name %>
    for <%= @listing.price %><%= credit_symbol %>?
  </p>

  <%= link_to "Pay #{@listing.price}#{credit_symbol}", confirm_purchase_listing_path(@building, @listing) %>
  <%= link_to 'Cancel', listings_path(@building) %>
<% end %>
